{
  "justification-note": "Each key is the exact Git blob object id (git hash-object) of the synthetic fixture file beside this manifest; each justification is reviewed in the diff that adds it. Fixture content is synthetic by construction: every token below is a documented example value from public provider docs, padded to the documented length, and no real credential has ever existed here."
}
